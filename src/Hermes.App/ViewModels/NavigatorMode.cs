namespace Hermes.App.ViewModels;

/// <summary>
/// Identifies which navigator panel is active in the activity bar.
/// </summary>
public enum NavigatorMode
{
    ActionItems,
    Documents,
    Threads,
    Timeline,
    Activity,
}
